// Bump alongside meaningful releases. Displayed in the bottom-left corner of
// the Dashboard so it's easy to eyeball whether the PWA has actually picked
// up a new deploy (see sw.js's network-first fetch strategy for why that
// isn't always obvious otherwise). Keep in sync with package.json's version.
export const APP_VERSION = '1.0.0';
