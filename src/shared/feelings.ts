/**
 * Quick-tap defaults for the finish-workout feeling picker, ordered bad to
 * good. Not exhaustive — the picker also accepts any custom emoji typed in
 * directly, so this list is just a starting point, not a constraint.
 */
export const FEELING_PRESETS = ['😓', '😐', '💪'];
